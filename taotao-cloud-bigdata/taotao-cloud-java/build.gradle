archivesBaseName = 'taotao-cloud-java'

jar.enabled = true
bootJar.enabled = false

dependencies {
    compile 'org.apache.activemq:activemq-all:5.11.2'

    implementation 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'
}
